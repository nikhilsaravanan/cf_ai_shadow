# Supabase auth — see README "Auth setup". The anon/publishable key is safe to
# expose in client bundles; the service role key must NEVER be put here.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxx
